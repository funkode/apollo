Exercise #23

1. Observe the following pattern:

const withMyMutation = graphql(MUTATION, {
  props: ({ mutate, ownProps }) => ({

  }),
})

2. Refactor the Car Form and Append/Insert Car operation to use a HOC

3. Ensure it works.